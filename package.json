{
  "name": "notion-export-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cleanup": "rm -rf output/**",
    "export-md-single": "rm -rf output/* && node ./src/export-single-md.js",
    "export-md-all": "rm -rf output/* && node ./src/export-all-md.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@notionhq/client": "^1.0.4",
    "dotenv": "^16.0.1",
    "notion-to-md": "^2.3.4",
    "pagedjs": "^0.3.5",
    "pagedjs-cli": "^0.1.6",
    "sanitize-filename": "^1.6.3"
  },
  "type": "module"
}
